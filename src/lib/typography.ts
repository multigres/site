/** Primary page title — matches the home hero heading. */
export const pageHeadingClassName =
  'font-heading font-normal text-4xl leading-[1.1] tracking-tight sm:text-5xl md:text-6xl';

/** Docs page title (`DocsTitle`). */
export const docPageHeadingClassName =
  'font-heading font-medium text-3xl leading-[1.15] tracking-tight sm:text-4xl';

/** Blog index and post metadata (date, author). */
export const blogDateClassName =
  'font-mono text-xs tabular-nums text-tertiary-foreground';
